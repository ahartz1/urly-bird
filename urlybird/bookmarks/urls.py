from django.conf.urls import include, url
from django.contrib import admin
from . import views as bviews


urlpatterns = [
    # url(r'^worms/(?P<pk>\w+)$', ),
    # url(r'^birds/$', ),
    # url(r'^clicks/$', ),
]
